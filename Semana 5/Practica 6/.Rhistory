install.packages("arules")
library("tidyverse")
library("arules")
data <- read.csv("groceries.csv", sep = ",")
View(data)
# lectura en formato basquet
mis_transacciones1 <- read.transactions("groceries.csv",
format = "basket",
sep = ",",      # Separador entre columnas (ID y Lista)
rm.duplicates = TRUE) # Limpieza: quita productos repetidos en una misma compra
# 1. Encontrar itemsets frecuentes con soporte >= 1% (0.001)
itemsets_frecuentes1 <- apriori(mis_transacciones1,
parameter = list(supp = 0.001, target = "frequent itemsets"))
View(itemsets_frecuentes1)
View(itemsets_frecuentes1)
summary(mis_transacciones1)
mis_transacciones1
items <- itemFrequency(mis_transacciones1)
summary(items)
# Ver las frecuencias relativas (soporte individual)
frec <- itemFrequency(mis_transacciones1, type = "relative")
sort(frec, decreasing = TRUE)[1:10]         # top 10 ítems más frecuentes
# Criterio estadístico para soporte mínimo
#    Se usa la MEDIA como umbral base, una práctica común:
#    "un ítem es relevante si aparece más que el ítem promedio"
soporte_min <- mean(frec)
cat("Soporte mínimo (media):", round(soporte_min, 4))
# 4. Ejecutar ECLAT con ese soporte mínimo
reglas_eclat <- eclat(
mis_transacciones1,
parameter = list(
support = soporte_min,   # umbral calculado
minlen  = 1,             # mínimo 1 ítem por conjunto
maxlen  = 10             # máximo ítems por conjunto
)
)
# 5. Ver los 5 itemsets de MAYOR soporte
top5 <- sort(reglas_eclat, by = "support", decreasing = TRUE)
inspect(head(top5, 5))
reglas_eclat_05 <- eclat(
mis_transacciones1,
parameter = list(
support = 0.5,   # umbral calculado
minlen  = 1,             # mínimo 1 ítem por conjunto
maxlen  = 10             # máximo ítems por conjunto
)
)
reglas_eclat_025 <- eclat(
mis_transacciones1,
parameter = list(
support = 0.025,   # umbral calculado
minlen  = 1,             # mínimo 1 ítem por conjunto
maxlen  = 10             # máximo ítems por conjunto
)
)
# Ver los 5 itemsets de MAYOR soporte
top5 <- sort(reglas_eclat_05, by = "support", decreasing = TRUE)
# Ver los 5 itemsets de MAYOR soporte
top5 <- sort(reglas_eclat_025, by = "support", decreasing = TRUE)
inspect(head(top5, 5))
# visualizamos la distribucion de los datos para elegir el estadistico:
boxplot(frec,
main  = "Comparación entre grupos",
xlab  = "Grupos",
ylab  = "Valores",
col   = c("lightblue", "lightgreen", "lightyellow"),
border = "darkgray")
# visualizamos la distribucion de los datos para elegir el estadistico:
boxplot(frec,
main  = "Distribucion de las frecuencias",
xlab  = "Frecuencias",
ylab  = "Valores",
col   = c("lightblue", "lightgreen", "lightyellow"),
border = "darkgray")
soporte_min <- median(frec)
# Ejecutar ECLAT con ese soporte mínimo
reglas_eclat <- eclat(
mis_transacciones1,
parameter = list(
support = soporte_min,   # umbral calculado
minlen  = 1,             # mínimo 1 ítem por conjunto
maxlen  = 10             # máximo ítems por conjunto
)
)
# Ver los 5 itemsets de MAYOR soporte
top5 <- sort(reglas_eclat, by = "support", decreasing = TRUE)
inspect(head(top5, 5))
# Inducir reglas desde los itemsets frecuentes
reglas1 <- ruleInduction(
top5,                          # itemsets base
transactions = trans,          # datos originales (para calcular confianza)
confidence   = 0.5             # confianza mínima
)
# Inducir reglas desde los itemsets frecuentes
reglas1 <- ruleInduction(
top5,                          # itemsets base
transactions = mis_transacciones1,          # datos originales (para calcular confianza)
confidence   = 0.5             # confianza mínima
)
inspect(sort(reglas, by = "confidence", decreasing = TRUE))
inspect(sort(reglas1, by = "confidence", decreasing = TRUE))
length(reglas1)
# Inducir reglas desde los itemsets frecuentes
reglas2 <- ruleInduction(
top5,                          # itemsets base
transactions = mis_transacciones1,          # datos originales (para calcular confianza)
confidence   = 0.025             # confianza mínima
)
length(reglas2)
inspect(sort(reglas2, by = "confidence", decreasing = TRUE))
# Inducir reglas desde los itemsets frecuentes
reglas2 <- ruleInduction(
top5,                          # itemsets base
transactions = mis_transacciones1,          # datos originales (para calcular confianza)
confidence   = 0.25             # confianza mínima
)
length(reglas2)
inspect(sort(reglas2, by = "confidence", decreasing = TRUE))
length(reglas1)
# Reglas que tiene un Lift mayor a 2 (lift > 2)
reglas_lift2 <- subset(reglas2, lift > 2)
inspect(sort(reglas_lift2, by = "lift", decreasing = TRUE))
length(reglas_lift2)
reglas_rhs <- subset(reglas2,
rhs %in% "instant coffee" &
lift > 2)
inspect(sort(reglas_rhs, by = "lift", decreasing = TRUE))
length(reglas_rhs)
reglas_rhs <- subset(reglas2,
rhs %in% "root vegetables" &
lift > 2)
inspect(sort(reglas_rhs, by = "lift", decreasing = TRUE))
length(reglas_rhs)
reglas_lhs <- subset(reglas2,
lhs %in% c("root vegetables","baby food") &
lift > 2)
inspect(sort(reglas_lhs, by = "lift", decreasing = TRUE))
length(reglas_lhs)
reglas_combinada <- subset(reglas2,
(lhs %in% "preservation products" |
lhs %in% "liqueur") &
rhs %in% "root vegetables" &
lift > 2)
inspect(sort(reglas_combinada, by = "lift", decreasing = TRUE))
length(reglas_combinada)
reglas_combinada <- subset(reglas2,
(lhs %in% "root vegetables" |
lhs %in% "liqueur") &
rhs %in% "other vegetables" &
lift > 2)
inspect(sort(reglas_combinada, by = "lift", decreasing = TRUE))
length(reglas_combinada)
rm() = list(ls)
rm(list = ls())
data <- read.csv("orders.csv", sep = ";")
View(data)
mis_transacciones1 <- read.transactions("orders.csv",
format = "single",
sep = ";",
cols = c("order_id", "product_name"),
header = TRUE)
library("tidyverse")
library("arules")
mis_transacciones1 <- read.transactions("orders.csv",
format = "single",
sep = ";",
cols = c("order_id", "product_name"),
header = TRUE)
summary(mis_transacciones1) # vemos si levanto bien todas las transacciones
mis_transacciones1
frec <- itemFrequency(mis_transacciones1, type = "relative")
sort(frec, decreasing = TRUE)[1:10]
items_frec_eclat <- eclat(mis_transacciones1,
parameter = list(support = 0.1),
control = list(verbose = F))
inspect(sort(items_frec_eclat, by = "support", decreasing = T)[1:10])
inspect(sort(items_frec_eclat, by = "support", decreasing = T)[1:10])
items_frec_eclat
inspect(sort(items_frec_eclat, by = "support", decreasing = T)[1:10])
View(items_frec_eclat)
items_frec_eclat <- eclat(mis_transacciones1,
parameter = list(support = 0.001),
control = list(verbose = F))
items_frec_eclat
inspect(sort(items_frec_eclat, by = "support", decreasing = T)[1:10])
items_frec_eclat <- eclat(mis_transacciones1,
parameter = list(support = 0.001, minlen = 3),
control = list(verbose = F))
items_frec_eclat
inspect(sort(items_frec_eclat, by = "support", decreasing = T)[1:10])
reglas_eclat <- ruleInduction(itemsets_frec_eclat,
transactions = transacciones,
confidence = 0.6)
reglas_eclat <- ruleInduction(items_frec_eclat,
transactions = transacciones,
confidence = 0.6)
reglas_eclat <- ruleInduction(items_frec_eclat,
mis_transacciones1,
confidence = 0.6)
reglas_ordenadas <- sort(reglas_eclat, by = "lift", decreasing = TRUE)
inspect(reglas_ordenadas)
reglasapriori <- apriori(mis_transacciones1,
parameter = list(support = 0.001,
confidence = 0.6,
target = "rules"),
control = list(verbose=F))
reglasapriori
reglas_eclat
inspect(reglasapriori) # 22 reglas
inspect(reglas_eclat) # 15 reglas
reglasapriori <- apriori(mis_transacciones1,
parameter = list(support = 0.001,
confidence = 0.6,
target = "rules", minlen = 3),
control = list(verbose=F))
reglasapriori
metricas_extra_eclat <- interestMeasure(reglas_eclat,
measure = c("coverage", "fishersExactTest"),
transactions = transacciones)
metricas_extra_apriori <- interestMeasure(reglas_apriori,
measure = c("coverage", "fishersExactTest"),
transactions = transacciones)
metricas_extra_eclat <- interestMeasure(reglas_eclat,
measure = c("coverage", "fishersExactTest"),
transactions = transacciones)
metricas_extra_apriori <- interestMeasure(reglasapriori,
measure = c("coverage", "fishersExactTest"),
transactions = transacciones)
# 2. Agregamos estas métricas a los objetos de reglas para poder visualizarlas
quality(reglas_eclat) <- cbind(quality(reglas_eclat), metricas_extra_eclat)
quality(reglasapriori) <- cbind(quality(reglasapriori), metricas_extra_apriori)
# 3. Comparación visual de las 5 mejores reglas por Lift
inspect(head(sort(reglas_eclat, by = "lift"), 5))
inspect(head(sort(reglasapriori, by = "lift"), 5))
# Filtrar las reglas generadas (puedes usar reglas_eclat o reglas_apriori)
reglas_filtradas <- subset(reglasapriori,
subset = rhs %in% "Bag of Organic Bananas" &
lhs %in% "Organic Raspberries")
# Ordenar por lift para priorizar las asociaciones más fuertes
reglas_filtradas <- sort(reglas_filtradas, by = "lift", decreasing = TRUE)
# Visualizar el resultado
inspect(reglas_filtradas)
# Visualizar el resultado
reglas_filtradas
# 2. Agregamos estas métricas a los objetos de reglas para poder visualizarlas
quality(reglas_eclat) <- cbind(quality(reglas_eclat), metricas_extra_eclat)
# Visualizar el resultado
summary(reglas_filtradas)
V
# Visualizar el resultado
View(reglas_filtradas)
# Visualizar el resultado
inspect(reglas_filtradas)
