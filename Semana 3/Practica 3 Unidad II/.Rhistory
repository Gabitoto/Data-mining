print("hello wordl")
library(readr)
merval <- read_csv("merval.csv")
View(merval)
View(merval)
install.packages("dplyr")
library(dplyr)
library(dplyr)
# 2. Crear el dataset ventaneado
merval_windowed <- merval %>%
mutate(
t_4 = lag(indice, 5), # Hace 5 días
t_3 = lag(indice, 4), # Hace 4 días
t_2 = lag(indice, 3), # Hace 3 días
t_1 = lag(indice, 2), # Hace 2 días
t   = lag(indice, 1), # Valor de hoy
target = indice       # Valor a predecir (mañana respecto a 't')
) %>%
# Al desplazar los datos, las primeras 5 filas tendrán valores NA
drop_na() %>%
# Reordenamos para que el target sea la última columna
select(t_4, t_3, t_2, t_1, t, target)
install.packages("tidyverse")
library(tidyverse)
# 2. Crear el dataset ventaneado
merval_windowed <- merval %>%
mutate(
t_4 = lag(indice, 5), # Hace 5 días
t_3 = lag(indice, 4), # Hace 4 días
t_2 = lag(indice, 3), # Hace 3 días
t_1 = lag(indice, 2), # Hace 2 días
t   = lag(indice, 1), # Valor de hoy
target = indice       # Valor a predecir (mañana respecto a 't')
) %>%
# Al desplazar los datos, las primeras 5 filas tendrán valores NA
drop_na() %>%
# Reordenamos para que el target sea la última columna
select(t_4, t_3, t_2, t_1, t, target)
# 3. Inspeccionar el resultado
print(head(merval_windowed))
library(data.table)
# 2. Definir los nombres de las nuevas columnas
nombres_lags <- c("t_5","t_4", "t_3", "t_2", "t_1", "t")
# 3. Crear las columnas de desfase y el target
# Usamos shift con un vector de offsets (6, 5, 4, 3, 2, 1)
merval[, (nombres_lags) := shift(indice, 6:1, type = "lag")]
# 1. Forzar la conversión a data.table (esto corrige el error)
dt <- fread("merval.csv", col.names = "indice")
# 2. Definir los nombres de las nuevas columnas
nombres_lags <- c("t_5","t_4", "t_3", "t_2", "t_1", "t")
# 3. Crear las columnas de desfase y el target
# Usamos shift con un vector de offsets (6, 5, 4, 3, 2, 1)
dt[, (nombres_lags) := shift(indice, 6:1, type = "lag")]
# 4. Renombrar la columna original como target y eliminar filas con NA
setnames(dt, "indice", "target")
dt_windowed <- na.omit(dt)
# 5. Reordenar las columnas para que el target quede al final
setcolorder(dt_windowed, c(nombres_lags, "target"))
# Ver el resultado
print(head(dt_windowed))
library(data.table)
# 1. Forzar la conversión a data.table (esto corrige el error)
dt <- fread("merval.csv", col.names = "indice")
setDT(dt)
# 2. Definir los nombres de las nuevas columnas
nombres_lags <- c("t_4", "t_3", "t_2", "t_1", "t")
# 3. Crear las columnas de desfase y el target
# Usamos shift con un vector de offsets ( 5, 4, 3, 2, 1)
dt[, (nombres_lags) := shift(indice, 5:1, type = "lag")]
# 4. Renombrar la columna original como target y eliminar filas con NA
setnames(dt, "indice", "target")
dt_windowed <- na.omit(dt)
# 5. Reordenar las columnas para que el target quede al final
setcolorder(dt_windowed, c(nombres_lags, "target"))
# Ver el resultado
print(head(dt_windowed))
library(readr)
Entrenamiento_ECI_2020 <- read_csv("Entrenamiento_ECI_2020.csv")
View(Entrenamiento_ECI_2020)
View(dt_windowed)
View(dt)
View(dt)
library(readr)
Entrenamiento_ECI_2020 <- read_csv("Entrenamiento_ECI_2020.csv")
View(Entrenamiento_ECI_2020
View(Entrenamiento_ECI_2020)
View(Entrenamiento_ECI_2020)
str(Entrenamiento_ECI_2020)
summary(Entrenamiento_ECI_2020)
isna(Entrenamiento_ECI_2020)
is.nan(Entrenamiento_ECI_2020)
# Si querés especificar el orden de los niveles (levels)
Entrenamiento_ECI_2020$Stage <- factor(Entrenamiento_ECI_2020$Stage, levels = c("Closed Lost", "Closed Won"))
str(Entrenamiento_ECI_2020)
View(Entrenamiento_ECI_2020)
Entrenamiento_ECI_2020$Stage
str(Entrenamiento_ECI_2020$Stage)
summary(Entrenamiento_ECI_2020$Stage)
# Eliminamos los NA que estan en Stage
library(data.table)
# Opción 1: Filtrado estándar
dataset_limpio <- Entrenamiento_ECI_2020[!is.na(Stage)]
# Opción 1: Filtrado estándar
dataset_limpio <- Entrenamiento_ECI_2020[!is.na(Stage)]
dataset_limpio <- na.omit(Entrenamiento_ECI_2020, cols = "Stage")
str(dataset_limpio$Stage)
summary(dataset_limpio$Stage)
dataset_limpio <- na.omit(Entrenamiento_ECI_2020)
dataset_limpio1 <- na.omit(Entrenamiento_ECI_2020,cols="Stage")
dataset_limpio1 <- na.omit(Entrenamiento_ECI_2020,cols=Entrenamiento_ECI_2020$Stage)
# Eliminamos los NA que estan en Stage
library(data.table)
# Convertir a data.table (esto es lo que probablemente falta)
setDT(Entrenamiento_ECI_2020)
# Ahora sí funcionará el argumento cols
dataset_limpio1 <- na.omit(Entrenamiento_ECI_2020, cols = "Stage")
dataset_limpio2 <- na.omit(Entrenamiento_ECI_2020, cols = "Stage")
library(readr)
Entrenamiento_ECI_2020 <- read_csv("Entrenamiento_ECI_2020.csv")
View(Entrenamiento_ECI_2020)
str(Entrenamiento_ECI_2020)
summary(Entrenamiento_ECI_2020)
is.nan(Entrenamiento_ECI_2020)
# Si querés especificar el orden de los niveles (levels)
Entrenamiento_ECI_2020$Stage <- factor(Entrenamiento_ECI_2020$Stage, levels = c("Closed Lost", "Closed Won"))
str(Entrenamiento_ECI_2020)
# Eliminamos los NA que estan en Stage
library(data.table)
# Convertir a data.table (esto es lo que probablemente falta)
setDT(Entrenamiento_ECI_2020)
# Ahora sí funcionará el argumento cols
dataset_limpio2 <- na.omit(Entrenamiento_ECI_2020, cols = "Stage")
View(dataset_limpio2)
library(ggplot2)
# Crear el boxplot
ggplot(dataset_limpio2, aes(x = Stage, y = Total_Amount, fill = Stage)) +
geom_boxplot(outlier.color = "red", outlier.shape = 1) +
labs(
title = "Distribución del Monto Total por Etapa (Stage)",
subtitle = "Análisis de variabilidad entre categorías",
x = "Etapa del Proceso (Stage)",
y = "Monto Total (Total Amount)"
) +
theme_minimal() +
theme(legend.position = "none") # Ocultamos la leyenda ya que el eje X ya indica el Stage
# Crear el boxplot
ggplot(dataset_limpio2, aes(x = Stage, y = Total_Amount, fill = Stage)) +
geom_boxplot(outlier.color = "red", outlier.shape = 1) +
labs(
title = "Distribución del Monto Total por Etapa (Stage)",
subtitle = "Análisis de variabilidad entre categorías",
x = "Etapa del Proceso (Stage)",
y = "Monto Total (Total Amount)"
) +
scale_y_log10() +
labs(y = "Monto Total (Escala Log10)") +
theme_minimal() +
theme(legend.position = "none") # Ocultamos la leyenda ya que el eje X ya indica el Stage
library(readr)
Entrenamiento_ECI_2020 <- read_csv("Entrenamiento_ECI_2020.csv")
library(readr)
Entrenamiento_ECI_2020 <- read_csv("Entrenamiento_ECI_2020.csv")
View(Entrenamiento_ECI_2020)
